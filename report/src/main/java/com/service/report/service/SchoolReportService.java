package com.service.report.service;

import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;

@Service
@RequiredArgsConstructor
public class SchoolReportService {

    //private final SchoolClient client;

    /*public Report generateUserReport(SchoolReportRequest reportRequest) {
        List<School> userList = userService.getUsersByCriteria(reportRequest);
        // Use a report generation library (e.g., JasperReports) to create the report
        // ...

        // Return the generated report
        return generatedReport;
    }*/
}
